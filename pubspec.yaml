name: mimicam
version: 1.0.0+1
publish_to: 'none'
description: LAN tabanli Flutter bebek kamera server/client uygulamasi.

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  camera: ^0.11.0+2
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.3
  qr_flutter: ^4.1.0
  web_socket_channel: ^3.0.1
  flutter_local_notifications: ^17.2.2
  wakelock_plus: ^1.2.8
  image: ^4.2.0
  record: ^7.1.0
  crypto: ^3.0.3
  mobile_scanner: ^7.2.0
  flutter_secure_storage: ^10.3.1
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/mimicam_launcher_icon.png
    - assets/branding/mimicam_wordmark.png
    - assets/test_dashboard/index.html
    - assets/test_dashboard/dashboard.js
